# --- CẤU HÌNH TẢI PYTORCH GPU (BẮT BUỘC) ---
--extra-index-url https://download.pytorch.org/whl/cu121

# --- CORE AI ENGINE (ĐỂ CHẠY MODEL) ---
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# --- HUGGING FACE & GPTQ (ĐỂ ĐỌC MODEL NÉN) ---
transformers>=4.43.3
accelerate>=0.32.0
optimum>=1.21.0
auto-gptq>=0.7.1

# --- API SERVER (THÀNH PHẦN MỚI) ---
fastapi          # Framework để tạo API
uvicorn          # Server để chạy API
pydantic         # Để kiểm tra dữ liệu đầu vào (User input)
requests         # (Tùy chọn) Để viết code test thử API

# --- TIỆN ÍCH ---
tqdm
